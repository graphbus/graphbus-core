networkx>=3.0

# Code-graph RAG — Tree-sitter code graph + Memgraph/JSON export
# Live mode (MemgraphIngestor) requires Docker; build requires cmake.
code-graph-rag[treesitter-full]

litellm>=1.0.0
pytest>=7.0.0
pytest-cov>=4.0.0

# CLI dependencies
click>=8.1.0
rich>=13.0.0
PyYAML>=6.0

# WebSocket server for UI communication (optional)
websockets>=12.0

# TUI dependencies (optional)
textual>=0.47.0

# HTTP client for remote negotiation backend
httpx>=0.25.0

# .env file loading
python-dotenv>=1.0.0

# Firebase Admin SDK (optional — for multi-tenant OAuth + API key management)
firebase-admin>=6.0.0
