# GraphBus API Server — Environment Variables

# ── Firestore / Firebase ─────────────────────────────────────────
# Path to service account JSON (defaults to ~/.config/graphbus/firebase-sa.json)
GOOGLE_APPLICATION_CREDENTIALS=~/.config/graphbus/firebase-sa.json

# Firebase project + Firestore database to use
FIREBASE_PROJECT_ID=spicy-chai
FIRESTORE_DATABASE=analytics

# Alternatively, paste the SA JSON inline:
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# ── Single-key fallback (self-hosted, no Firebase) ────────────────
# Auto-generated on first start if Firebase is not configured
# GRAPHBUS_API_KEY=

# ── LLM (server does NOT make LLM calls — clients bring their own keys) ──
# N/A

# ── Server ────────────────────────────────────────────────────────
# PORT=8080
