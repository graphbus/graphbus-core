name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13']

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        # Cache the pip download cache so repeated runs don't re-download
        # packages that haven't changed.  setup-python@v5 derives the cache
        # key from requirements.txt automatically when cache: 'pip' is set.
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run unit tests
      run: |
        pytest tests/unit/ -v --cov=graphbus_core --cov-report=xml --cov-report=term

    - name: Run functional tests
      run: |
        pytest tests/functional/ -v

    - name: Run integration tests (no LLM)
      run: |
        pytest tests/integration/ -v -m "not llm and not slow"

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false
